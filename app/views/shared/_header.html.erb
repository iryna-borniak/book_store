<nav class="main-nav">
  <div class="nav-container">
    <%= link_to "e-library", books_path, class: "logo" %>

    <div class="flex gap-x-2">
      <% if user_signed_in? %>        
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn-blue" %>        
      <% else %>
        <div class="flex gap-x-2">
          <%= link_to "Log In", new_user_session_path, class: "btn-blue" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn-blue" %>
        </div>
      <% end %>

      <%= link_to "New book", new_book_path, class: "btn-blue" %>
    </div>
  </div>
</nav>
